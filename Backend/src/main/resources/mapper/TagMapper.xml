<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.newha.mapper.TagMapper">

	<select id="selectAll" parameterType="String" resultType="HashTag">
		select * from hashtag where name like #{keyword}
	</select>
</mapper>